pipeline {
    agent any
    stages {
        stage("init") {
            steps {
                checkout scm
            }
        }

        stage("build") {
            echo "Building"
        }
    }
}